<%
/* generates a basic "this Gecko interface implements this standard DOM interface" page */
/* two parameters: Gecko interface name, DOM interface name */

/* get the page language */
var lang = env.locale;
var str = "";

switch(lang) {
    case "ja":
        str = template("interface", [$0]) + "インターフェースには" + template("domxref", [$1]) + "インターフェースが実装されています。詳細については各ページを参照して下さい。";
        break;
    default:
        str = "The " + template("interface", [$0]) + " interface implements the DOM " + template("domxref", [$1]) + " interface. See that page for details.";
        break;
}


%><%- str %>
