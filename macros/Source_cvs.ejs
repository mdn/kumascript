<%
// Insert a link to a file in the CVS Mozilla source repository.
//
//  $0 - File to link to
//  $1 - (OPTIONAL) text for the link
//
// If $1 isn't provided, the link itself is the link text

var url = "https://dxr.mozilla.org/mozilla/source/" + $0;
var text = ($1 || url);
%>
<a rel="custom" href="<%=url%>"><%=text%></a>
