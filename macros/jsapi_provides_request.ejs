<%
var locale = env.locale;
var str = "Provides request. <em>In <code><a href=\"/" + locale + "/docs/JS_THREADSAFE#Requests\">JS_THREADSAFE</a></code> builds, the JavaScript engine calls this callback only from within an active request on <code>cx</code>.  The callback does not need to call <code>JS_BeginRequest()</code>).</em>";

switch (locale) {
    case "uk":
      str = "Надає запит. <em>У збрірках <code><a href=\"/" + locale + "/docs/JS_THREADSAFE#Requests\">JS_THREADSAFE</a></code> рушій JavaScript здійснює цей зворотний виклик лише зсередини активного запиту у контексті <code>cx</code>. Зворотний виклик не має потреби здійснювати виклик <code>JS_BeginRequest()</code>).</em>";
      break;
}
%>

<%- str %>
