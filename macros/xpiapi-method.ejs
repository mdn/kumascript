<% /*

for only French

    $0 : slug (name of object)
    $1 : slug & text (name of method)

*/ %><code><%-
    template("mediawiki.internal", ['Référence_de_l\'API XPInstall/Objet_' + $0 + '/Méthodes/' + $1 + '|' + $1, "fr"])
%></code>
