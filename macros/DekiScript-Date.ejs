<% module.exports = buildAPI({
    now: function () {
        return (new Date());
    },
    
    parse: function(str) {
        return (Date.parse(str));
    },
    
    getLocaleDateString: function(d, locales, options) {
        var theDate = new Date(d);
        return theDate.toLocaleDateString(locales, options);
    }
}); %>
