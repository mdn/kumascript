<%
var locale = env.locale;
var text = "<code>JSObjectOps</code> is not a supported API. Details of the API may change from one release to the next. " +
           "This documentation should be considered SpiderMonkey internals documentation, not API documentation. See " +
           template("Bug", ["408416"]) + " for details.";

switch (locale) {
  case "uk":
      text = "<code>JSObjectOps</code> не є підтримуваним API. Певні зміни можуть відбуватися від одного випуску до іншого. " +
             "Вважайте ці статті документацією з нутрощів SpiderMonkey, а не документацією з API. Детальніше: " +
             template("Bug", ["408416"]) + ".";
    break;
}

%><%-template("Warning", [text])%>
